export interface EnvironmentInterface {
  production: boolean,
  stage: boolean,
  apiDomain: string,
  host: string,
  imageDomain: string,
  fileDomain: string,
  adminEmail:string,
  itemsPerPage:number
}
